<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>

<body>

    <div class="w-full min-h-screen p-10 bg-zinc-900 text-white">
        <div class="flex justify-end ">
            <a class="px-3 py-2 bg-red-500 rounded-md" href="/login">Logout</a>
        </div>
        <h1 class="text-2xl mb-2 "><span class="font-light">Hello,</span>
            <%= user.name %>
        </h1>
        <h5 class="mb-5">you can create a new task.</h5>
        <form class="" action="/create" method="post">
            <input class="block mb-3 px-3 py-2 w-1/5 bg-transparent rounded-md outline-none border-2 border-zinc-800 " type="text" name="title" placeholder="Title">
            <input class="p-3 resize-none w-1/3  bg-transparent rounded-md outline-none border-2 border-zinc-800" type="text" name="description" placeholder="Description">
            <input class="px-5 py-2 bg-blue-500 rounded-md block mt-3" type="submit" value="Create task">
        </form>
        <div>
            <h3 class="text-zinc-400 mt-10 text-2xl">Your Tasks</h3>
            <div class="taskcontainer mt-5 flex gap-3">
                <% if(tasks.length > 0){ %>
                    <% tasks.forEach(task => { %>
                        <div class="w-1/3 p-4 mt-3  rounded-md border-[1px] border-zinc-700 bg-zinc-800">
                            <div class="flex justify-between">
                                <h1 class="text-xl text-gray-400 mb-3"><%= task.title%></h1>
                                <% if(task.completed){ %>
                                <span class="text-green-500">Completed</span>
                                  <% } else { %>
                                    <span class="text-yellow-400">Pending</span>
                                <% } %>
                            </div>
                            <p class="text-sm tracking-tight"><%= task.description %></p>
                            <div class="flex items-center mt-5 gap-3">
                                <a class="text-sm text-blue-400" href="/task/<%= task._id %>/edit">Edit task</a>
                                <form action="/task/<%= task._id %>/delete" method="post" class="inline">
                                    <button type="submit" class="text-sm text-red-400 ">Delete</button>
                                </form>
                            </div>
                        </div>
                        <% }) %>
                     <% } else { %>
                    <h1 class="text-sm text-zinc-400">No tasks found</h1>
                <% } %>
            </div>
        </div>
    </div>
</body>

</html>